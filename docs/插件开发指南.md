# AICowork 插件开发指南

> **版本**: v0.1.0
> **作者**: Alan <none>
> **创建日期**: 2026-01-24
> **最后更新**: 2026-01-24

---

## 📋 目录

1. [插件系统概述](#插件系统概述)
2. [技能插件开发](#技能插件开发)
3. [MCP 服务器开发](#mcp-服务器开发)
4. [代理插件开发](#代理插件开发)
5. [插件测试](#插件测试)
6. [插件发布](#插件发布)

---

## 插件系统概述

### 插件类型

AICowork 支持三种类型的插件：

| 插件类型 | 复杂度 | 功能 | 适用场景 |
|---------|-------|------|---------|
| **技能插件** | ⭐ | 提示词模板 | 快速扩展 AI 能力 |
| **MCP 服务器** | ⭐⭐⭐ | 外部工具集成 | 连接外部服务和 API |
| **代理插件** | ⭐⭐ | AI 专家角色 | 专业领域的任务处理 |

---

## 技能插件开发

### 什么是技能插件？

技能插件是基于 **Markdown + Frontmatter** 的轻量级插件，通过预定义的提示词模板扩展 AI 的能力。

### 技能文件结构

```markdown
---
name: skill-name
description: 技能描述
author: 作者名称
version: 1.0.0
parameters:
  - name: param1
    description: 参数1描述
    required: true
---

# 技能提示词内容

你是一个[角色]，你的任务是[任务描述]。

## 参数说明
- 参数1：{{param1}}
- 参数2：{{param2}}

## 执行步骤
1. 第一步
2. 第二步
3. 第三步

## 输出格式
请按照以下格式输出...
```

---

### 快速开始

#### 步骤 1: 创建技能文件

在 `~/.claude/skills/` 目录创建文件 `my-skill.md`：

```markdown
---
name: summarizer
description: 文本摘要生成器
author: Your Name
version: 1.0.0
parameters:
  - name: text
    description: 需要摘要的文本
    required: true
  - name: length
    description: 摘要长度（short/medium/long）
    required: false
---

# 文本摘要专家

你是一位专业的文本摘要专家，擅长提取文本的核心信息。

## 任务说明

请对用户提供的文本生成摘要，控制摘要长度。

## 参数说明
- 原文：{{text}}
- 摘要长度：{{length}}

## 摘要原则

1. **保留关键信息** - 保留核心观点和重要数据
2. **简洁明了** - 使用简洁的语言表达
3. **逻辑清晰** - 保持原文的逻辑结构
4. **客观中立** - 不添加个人观点

## 输出格式

根据用户选择的长度，输出摘要：

### short（50字以内）
简明扼要地概括核心内容。

### medium（150字以内）
包含主要观点和关键细节。

### long（300字以内）
详细的摘要，保留重要论据和数据。

请开始生成摘要。
```

---

#### 步骤 2: 使用技能

在 AICowork 中输入：

```
/input summarizer --text="这是一段很长的文本..." --length="short"
```

---

### 技能开发最佳实践

#### 1. 清晰的角色定位

```markdown
# 代码审查专家

你是一位经验丰富的代码审查专家，拥有 10 年以上的开发经验。
你精通多种编程语言，熟悉各种设计模式和最佳实践。
```

#### 2. 明确的任务说明

```markdown
## 任务说明

你的任务是审查用户提交的代码，从以下几个方面提供反馈：

1. **代码风格** - 检查格式、命名、缩进
2. **潜在问题** - 识别 Bug、边界条件
3. **性能优化** - 指出性能瓶颈
4. **安全性** - 检查输入验证、安全漏洞
5. **最佳实践** - 推荐更好的实现方式
```

#### 3. 结构化的输出格式

```markdown
## 输出格式

请按照以下结构输出审查报告：

### ✅ 优点
- 列出代码的优点

### ⚠️ 问题
- 列出发现的问题
- 提供修复建议

### 💡 优化建议
- 提供性能优化建议

### 🔒 安全建议
- 指出潜在的安全风险
```

#### 4. 参数验证

```markdown
## 参数说明

- 代码片段：{{code}}
  - 必需参数
  - 应为完整的代码片段

- 编程语言：{{lang}}
  - 必需参数
  - 支持：JavaScript, Python, Java, Go, Rust

- 关注点：{{focus}}
  - 可选参数
  - 可选值：style, performance, security, all
```

---

### 高级技能示例

#### 示例 1: 邮件生成器

```markdown
---
name: email-generator
description: 专业邮件生成器
author: Your Name
version: 1.0.0
parameters:
  - name: recipient
    description: 收件人
    required: true
  - name: subject
    description: 邮件主题
    required: true
  - name: purpose
    description: 邮件目的
    required: true
  - name: tone
    description: 语气风格
    required: false
---

# 商务邮件专家

你是一位专业的商务邮件撰写专家。

## 邮件要素

- **收件人**：{{recipient}}
- **主题**：{{subject}}
- **目的**：{{purpose}}
- **语气**：{{tone}}

## 邮件结构

### 主题行
简短有力，突出重点

### 称呼
根据收件人和语气选择

### 开场白
简短问候，说明邮件目的（1-2句话）

### 正文
- 使用项目符号列表
- 每段一个主题
- 保持简洁

### 结束语
明确的行动呼吁或期望

### 署名
专业的署名格式

## 撰写原则

1. **KISS 原则** - Keep It Short and Simple
2. **专业礼貌** - 使用商务用语
3. **行动导向** - 明确下一步行动
4. **校对无误** - 语法、拼写、格式

请开始撰写邮件。
```

---

#### 示例 2: API 文档生成器

```markdown
---
name: api-doc-generator
description: API 文档自动生成器
author: Your Name
version: 1.0.0
parameters:
  - name: code
    description: 代码片段
    required: true
  - name: lang
    description: 编程语言
    required: true
  - name: format
    description: 文档格式
    required: false
---

# API 文档专家

你是一位 API 文档专家，擅长为代码生成清晰、完整的文档。

## 代码信息

- **编程语言**：{{lang}}
- **代码片段**：
```{{lang}}
{{code}}
```

- **输出格式**：{{format}}

## 文档结构

### 1. 功能概述
简要描述函数/方法的功能和用途（2-3句话）

### 2. 语法
```{{lang}}
function name(param1: Type1, param2: Type2): ReturnType
```

### 3. 参数说明

| 参数 | 类型 | 必需 | 默认值 | 说明 |
|-----|------|-----|-------|------|
| param1 | Type1 | 是 | - | 参数1说明 |
| param2 | Type2 | 否 | default | 参数2说明 |

### 4. 返回值
- **类型**：ReturnType
- **说明**：返回值的含义和可能的情况

### 5. 异常
- **ErrorType1**: 抛出条件
- **ErrorType2**: 抛出条件

### 6. 使用示例

#### 基础用法
```{{lang}}
// 示例代码
```

#### 高级用法
```{{lang}}
// 高级示例
```

### 7. 注意事项
- 特殊行为说明
- 边界条件
- 性能考虑
- 兼容性说明

## 生成原则

1. **准确完整** - 涵盖所有参数和返回值
2. **示例驱动** - 提供实际可用的代码示例
3. **清晰明了** - 使用简洁的语言
4. **格式统一** - 遵循文档规范

请开始生成文档。
```

---

## MCP 服务器开发

### 什么是 MCP？

**MCP (Model Context Protocol)** 是一个开放协议，允许 AI 应用连接到外部数据源和工具。

### MCP 服务器架构

```
┌─────────────────────────────────────────────────────────┐
│                    AICowork                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  MCP 客户端   │  │  AI Agent    │  │  用户界面     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                         ↕ STDIO/HTTP
┌─────────────────────────────────────────────────────────┐
│                    MCP 服务器                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  工具定义     │  │  资源管理     │  │  提示词模板   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                         ↕
┌─────────────────────────────────────────────────────────┐
│                    外部服务                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ GitHub   │  │ Database │  │  API     │  ...        │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
```

---

### 快速开始：使用 Python 创建 MCP 服务器

#### 步骤 1: 安装 MCP SDK

```bash
pip install mcp
```

#### 步骤 2: 创建服务器

创建文件 `my_mcp_server.py`：

```python
#!/usr/bin/env python3
"""
我的 MCP 服务器
提供自定义工具给 AI 使用
"""

from mcp.server import Server
from mcp.types import Tool, TextContent
import asyncio

# 创建服务器实例
server = Server("my-mcp-server")

@server.list_tools()
async def list_tools() -> list[Tool]:
    """列出所有可用工具"""
    return [
        Tool(
            name="calculate",
            description="执行数学计算",
            inputSchema={
                "type": "object",
                "properties": {
                    "expression": {
                        "type": "string",
                        "description": "数学表达式，如 '2 + 2'"
                    }
                },
                "required": ["expression"]
            }
        ),
        Tool(
            name="get_weather",
            description="获取天气信息",
            inputSchema={
                "type": "object",
                "properties": {
                    "city": {
                        "type": "string",
                        "description": "城市名称"
                    }
                },
                "required": ["city"]
            }
        )
    ]

@server.call_tool()
async def call_tool(name: str, arguments: dict) -> list[TextContent]:
    """调用工具"""

    if name == "calculate":
        # 执行计算
        try:
            result = eval(arguments["expression"])
            return [TextContent(
                type="text",
                text=f"计算结果: {result}"
            )]
        except Exception as e:
            return [TextContent(
                type="text",
                text=f"计算错误: {str(e)}"
            )]

    elif name == "get_weather":
        # 获取天气（示例）
        city = arguments["city"]
        # 这里可以调用真实的天气 API
        return [TextContent(
            type="text",
            text=f"{city} 今天天气：晴，温度 25°C"
        )]

    else:
        return [TextContent(
            type="text",
            text=f"未知工具: {name}"
        )]

# 启动服务器
async def main():
    from mcp.server.stdio import stdio_server

    async with stdio_server() as (read_stream, write_stream):
        await server.run(
            read_stream,
            write_stream,
            server.create_initialization_options()
        )

if __name__ == "__main__":
    asyncio.run(main())
```

---

#### 步骤 3: 在 AICowork 中配置

1. 打开设置 → MCP 服务器
2. 点击"添加服务器"
3. 填写配置：

```json
{
  "id": "my-mcp-server",
  "name": "我的 MCP 服务器",
  "command": "python",
  "args": ["/path/to/my_mcp_server.py"],
  "env": {}
}
```

4. 点击"测试连接"
5. 启用服务器

---

#### 步骤 4: 使用 MCP 工具

在对话中，AI 可以自动调用你定义的工具：

```
用户：帮我计算 123 * 456

AI: [调用 calculate 工具]
计算结果: 56088
```

---

### MCP 服务器最佳实践

#### 1. 错误处理

```python
@server.call_tool()
async def call_tool(name: str, arguments: dict) -> list[TextContent]:
    try:
        # 工具逻辑
        result = execute_tool(name, arguments)
        return [TextContent(type="text", text=str(result))]
    except Exception as e:
        # 返回错误信息
        return [TextContent(
            type="text",
            text=f"错误: {str(e)}"
        )]
```

#### 2. 输入验证

```python
@server.call_tool()
async def call_tool(name: str, arguments: dict) -> list[TextContent]:
    # 验证必需参数
    required = ["param1", "param2"]
    missing = [p for p in required if p not in arguments]

    if missing:
        return [TextContent(
            type="text",
            text=f"缺少必需参数: {', '.join(missing)}"
        )]

    # 执行工具逻辑
    ...
```

#### 3. 资源管理

```python
from mcp.types import Resource, TextContent

@server.list_resources()
async def list_resources() -> list[Resource]:
    """列出可用资源"""
    return [
        Resource(
            uri="file:///config.json",
            name="配置文件",
            description="应用配置",
            mimeType="application/json"
        )
    ]

@server.read_resource()
async def read_resource(uri: str) -> str:
    """读取资源内容"""
    if uri == "file:///config.json":
        with open("config.json", "r") as f:
            return f.read()
    else:
        raise ValueError(f"未知资源: {uri}")
```

---

### 使用 TypeScript 创建 MCP 服务器

```typescript
// my-mcp-server.ts
import { Server } from "@modelcontextprotocol/sdk/server/index.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
import {
  CallToolRequestSchema,
  ListToolsRequestSchema,
} from "@modelcontextprotocol/sdk/types.js";

// 创建服务器
const server = new Server(
  {
    name: "my-mcp-server",
    version: "1.0.0",
  },
  {
    capabilities: {
      tools: {},
    },
  }
);

// 列出工具
server.setRequestHandler(ListToolsRequestSchema, async () => {
  return {
    tools: [
      {
        name: "calculate",
        description: "执行数学计算",
        inputSchema: {
          type: "object",
          properties: {
            expression: {
              type: "string",
              description: "数学表达式",
            },
          },
          required: ["expression"],
        },
      },
    ],
  };
});

// 调用工具
server.setRequestHandler(CallToolRequestSchema, async (request) => {
  const { name, arguments: args } = request.params;

  if (name === "calculate") {
    const expression = args?.expression as string;
    try {
      const result = eval(expression);
      return {
        content: [
          {
            type: "text",
            text: `计算结果: ${result}`,
          },
        ],
      };
    } catch (error) {
      return {
        content: [
          {
            type: "text",
            text: `计算错误: ${error}`,
          },
        ],
      };
    }
  }

  throw new Error(`未知工具: ${name}`);
});

// 启动服务器
async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
}

main().catch(console.error);
```

---

## 代理插件开发

### 什么是 AI 代理？

AI 代理是具有特定角色和能力的 AI 专家，可以独立完成特定领域的任务。

### 代理定义

```typescript
interface Agent {
  id: string;              // 代理唯一 ID
  name: string;            // 代理名称
  description: string;     // 代理描述
  systemPrompt: string;    // 系统提示词
  tools?: Tool[];          // 可用工具列表
  orchestration: 'parallel' | 'sequential' | 'dynamic';  // 编排模式
}
```

---

### 快速开始：创建代码审查代理

#### 步骤 1: 创建代理配置

在 `~/.claude/agents/` 目录创建 `code-reviewer.json`：

```json
{
  "id": "code-reviewer",
  "name": "代码审查专家",
  "description": "专业的代码审查代理，从多个维度检查代码质量",
  "systemPrompt": "你是一位经验丰富的代码审查专家...\n\n## 审查维度\n1. 代码风格\n2. 潜在问题\n3. 性能优化\n4. 安全性\n5. 最佳实践",
  "tools": [
    {
      "name": "read_file",
      "description": "读取文件内容"
    },
    {
      "name": "search_code",
      "description": "搜索代码"
    }
  ],
  "orchestration": "parallel"
}
```

#### 步骤 2: 创建代理团队

创建 `code-review-team.json`：

```json
{
  "id": "code-review-team",
  "name": "代码审查团队",
  "description": "多维度代码审查团队",
  "agents": [
    {
      "id": "style-reviewer",
      "name": "风格审查员",
      "systemPrompt": "你负责检查代码风格和格式..."
    },
    {
      "id": "security-reviewer",
      "name": "安全审查员",
      "systemPrompt": "你负责检查代码安全性..."
    },
    {
      "id": "performance-reviewer",
      "name": "性能审查员",
      "systemPrompt": "你负责检查代码性能..."
    }
  ],
  "orchestration": "parallel"
}
```

#### 步骤 3: 使用代理

在对话中：

```
用户：请审查这段代码
[粘贴代码]

AI: [调用代码审查团队]
[返回综合审查报告]
```

---

### 代理编排模式

#### 1. 并行模式

多个代理同时工作，适合独立任务：

```json
{
  "orchestration": "parallel",
  "agents": ["style-reviewer", "security-reviewer", "performance-reviewer"]
}
```

流程：
```
用户请求
    ↓
┌───┴───┬─────────┬───────┐
│       │         │       │
代理1   代理2    代理3  代理N
│       │         │       │
└───┬───┴─────────┴───────┘
    ↓
汇总结果
```

#### 2. 顺序模式

代理按顺序执行，适合流程任务：

```json
{
  "orchestration": "sequential",
  "agents": ["draft-writer", "editor", "publisher"]
}
```

流程：
```
用户请求 → 代理1 → 代理2 → 代理3 → 最终结果
```

#### 3. 动态模式

根据任务自动选择代理：

```json
{
  "orchestration": "dynamic",
  "agents": ["coder", "writer", "analyst"],
  "router": {
    "type": "intent-based",
    "rules": [
      { "intent": "coding", "agent": "coder" },
      { "intent": "writing", "agent": "writer" },
      { "intent": "analysis", "agent": "analyst" }
    ]
  }
}
```

---

### 代理最佳实践

#### 1. 明确的角色定义

```json
{
  "systemPrompt": "你是一位资深的 Python 开发专家，\n\n## 你的专长\n- Python 生态系统\n- 性能优化\n- 最佳实践\n\n## 你的任务\n审查 Python 代码，提供改进建议"
}
```

#### 2. 合理的工具配置

```json
{
  "tools": [
    { "name": "read_file", "description": "读取文件" },
    { "name": "execute_code", "description": "执行代码" },
    { "name": "search_docs", "description": "搜索文档" }
  ]
}
```

#### 3. 清晰的输出格式

```json
{
  "systemPrompt": "...\n\n## 输出格式\n### 审查结果\n- 问题1\n- 问题2\n### 建议\n- 建议1\n- 建议2"
}
```

---

## 插件测试

### 技能测试

```bash
# 1. 创建测试文件
echo "测试输入" | /input my-skill --param="value"

# 2. 验证输出
# 检查 AI 是否正确执行了技能逻辑
```

### MCP 服务器测试

```bash
# 1. 启动服务器
python my_mcp_server.py

# 2. 在另一个终端测试
echo '{"jsonrpc": "2.0", "method": "tools/list"}' | python -m json.tool
```

### 代理测试

```bash
# 在 AICowork 中创建测试对话
# 输入任务，验证代理响应
```

---

## 插件发布

### 1. 准备发布

- [ ] 完善文档
- [ ] 添加示例
- [ ] 测试功能
- [ ] 编写 README

### 2. 发布到 GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/your-username/your-plugin.git
git push -u origin main
```

### 3. 发布到 NPM (可选)

```bash
npm login
npm publish
```

---

**文档维护**: Alan
**最后更新**: 2026-01-24
**许可证**: AGPL-3.0
