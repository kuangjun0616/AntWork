# AICowork 开发者文档

> **版本**: v0.1.0
> **作者**: Alan <none>
> **创建日期**: 2026-01-24
> **最后更新**: 2026-01-24
> **技术栈**: Electron + React 19 + TypeScript + SQLite

---

## 📋 目录

1. [项目概述](#项目概述)
2. [技术架构](#技术架构)
3. [开发环境搭建](#开发环境搭建)
4. [项目结构详解](#项目结构详解)
5. [核心模块说明](#核心模块说明)
6. [开发指南](#开发指南)
7. [测试指南](#测试指南)
8. [构建与打包](#构建与打包)
9. [调试技巧](#调试技巧)
10. [常见问题](#常见问题)

---

## 项目概述

### 核心特性

**AICowork** 是一款基于 Electron + React 19 构建的现代化桌面 AI 智能协作助手，主要特性包括：

- 🤖 **多厂商 AI API 支持** - 集成 60+ 主流 AI 服务商
- 🧠 **本地记忆系统** - 基于向量数据库的知识管理
- 🔧 **技能扩展系统** - 自定义技能 + MCP 协议
- 🤖 **AI 代理编排** - 并行/顺序/动态代理编排
- 🛡️ **隐私安全保护** - 数据本地存储，API Key 加密

### 技术选型理由

| 技术 | 选型理由 |
|------|----------|
| **Electron 39** | 成熟的跨平台桌面框架，丰富的生态系统 |
| **React 19** | 最新并发特性，更好的性能和开发体验 |
| **TypeScript** | 类型安全，减少运行时错误 |
| **Zustand** | 轻量级状态管理，简洁的 API |
| **better-sqlite3** | 同步 API，高性能的嵌入式数据库 |
| **Memvid SDK** | 强大的向量数据库，支持语义搜索 |

---

## 技术架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                     用户界面层 (UI)                          │
│           React 19 + Tailwind CSS + i18next                 │
│     ┌──────────┐  ┌──────────┐  ┌──────────┐               │
│     │ 聊天界面 │  │ 设置页面 │  │ 插件管理 │               │
│     └──────────┘  └──────────┘  └──────────┘               │
└─────────────────────────────────────────────────────────────┘
                           ↕ IPC (contextBridge)
┌─────────────────────────────────────────────────────────────┐
│                   预加载层 (Preload)                         │
│            安全 API 暴露 + 类型定义                          │
└─────────────────────────────────────────────────────────────┘
                           ↕ IPC Communication
┌─────────────────────────────────────────────────────────────┐
│                    主进程层 (Main)                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ API 代理层   │  │ 会话管理     │  │ 存储层       │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ 配置管理     │  │ 记忆系统     │  │ 插件系统     │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                           ↕
┌─────────────────────────────────────────────────────────────┐
│                      数据层 (Data)                           │
│    better-sqlite3 (会话) + Memvid (记忆向量)                │
└─────────────────────────────────────────────────────────────┘
```

### 技术栈版本

```json
{
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.17",
    "@anthropic-ai/tokenizer": "^0.0.4",
    "@memvid/sdk": "^2.0.120",
    "better-sqlite3": "^12.6.2",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "zustand": "^5.0.10",
    "winston": "^3.19.0"
  },
  "devDependencies": {
    "electron": "^39.2.7",
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.17"
  }
}
```

---

## 开发环境搭建

### 前置要求

- **Node.js**: >= 18.0.0
- **pnpm**: >= 8.0.0 (推荐使用 pnpm)
- **Git**: 最新版本
- **Python**: 3.x (用于编译 better-sqlite3)

### 安装步骤

#### 1. 克隆仓库

```bash
git clone https://github.com/Pan519/AICowork.git
cd AICowork
```

#### 2. 安装 pnpm（如未安装）

```bash
npm install -g pnpm
```

#### 3. 安装项目依赖

```bash
pnpm install
```

#### 4. 重建原生模块

```bash
pnpm rebuild
```

**⚠️ 注意**: 如果遇到 `better-sqlite3` 编译错误，可能需要：

```bash
# Windows
npm install -g windows-build-tools

# macOS
xcode-select --install

# Linux
sudo apt-get install build-essential
```

#### 5. 启动开发模式

```bash
pnpm dev
```

这将同时启动：
- **Vite 开发服务器** (http://localhost:5173)
- **Electron 主进程**

### 开发工具推荐

- **IDE**: VS Code + ESLint + Prettier
- **React DevTools**: Chrome 扩展
- **Electron DevTools**: 内置开发者工具

---

## 项目结构详解

### 完整目录树

```
AICowork/
├── src/
│   ├── electron/              # Electron 主进程
│   │   ├── main.ts           # 主入口文件
│   │   ├── preload.cts       # 预加载脚本
│   │   ├── ipc-handlers.ts   # IPC 事件处理器
│   │   ├── logger.ts         # Winston 日志系统
│   │   ├── pathResolver.ts   # 路径解析工具
│   │   ├── error-handling.ts # 全局错误处理
│   │   │
│   │   ├── api-proxy/        # API 代理层
│   │   │   └── index.ts      # AI API 代理
│   │   │
│   │   ├── handlers/         # 业务处理器
│   │   │   ├── session.handler.ts    # 会话处理
│   │   │   ├── message.handler.ts    # 消息处理
│   │   │   └── memory.handler.ts     # 记忆处理
│   │   │
│   │   ├── libs/             # 核心库（业务逻辑）
│   │   │   ├── config-store.ts       # API 配置管理
│   │   │   ├── session-store.ts      # 会话存储
│   │   │   ├── memory-tools.ts       # 记忆系统（Memvid + FileSystem）
│   │   │   ├── skills-store.ts       # 技能管理
│   │   │   ├── agents-store.ts       # AI 代理管理
│   │   │   ├── mcp-store.ts          # MCP 服务器管理
│   │   │   └── prompt-manager.ts     # 提示词管理
│   │   │
│   │   ├── managers/         # 管理器
│   │   │   ├── api-manager.ts        # API 管理器
│   │   │   └── db-manager.ts         # 数据库管理器
│   │   │
│   │   ├── services/         # 服务层
│   │   │   ├── anthropic.service.ts  # Anthropic API
│   │   │   └── openai.service.ts     # OpenAI API
│   │   │
│   │   ├── storage/          # 存储层
│   │   │   ├── database.ts            # SQLite 数据库
│   │   │   └── fs-storage.ts          # 文件系统存储
│   │   │
│   │   ├── middleware/       # 中间件
│   │   │   └── auth.middleware.ts     # 权限中间件
│   │   │
│   │   ├── config/           # 配置文件
│   │   │   └── providers.ts           # AI 提供商配置
│   │   │
│   │   └── types.ts          # 类型定义
│   │
│   ├── ui/                   # React 渲染进程
│   │   ├── main.tsx          # React 入口
│   │   ├── App.tsx           # 根组件
│   │   ├── App.css           # 全局样式
│   │   │
│   │   ├── components/       # UI 组件
│   │   │   ├── Chat/                 # 聊天组件
│   │   │   │   ├── ChatWindow.tsx
│   │   │   │   ├── MessageList.tsx
│   │   │   │   └── InputBox.tsx
│   │   │   ├── Settings/             # 设置组件
│   │   │   │   ├── ApiConfig.tsx
│   │   │   │   ├── MemorySystem.tsx
│   │   │   │   └── McpServers.tsx
│   │   │   └── Common/               # 通用组件
│   │   │       ├── Button.tsx
│   │   │       ├── Modal.tsx
│   │   │       └── Dropdown.tsx
│   │   │
│   │   ├── pages/            # 页面组件
│   │   │   ├── ChatPage.tsx
│   │   │   ├── SettingsPage.tsx
│   │   │   └── PluginsPage.tsx
│   │   │
│   │   ├── store/            # Zustand 状态管理
│   │   │   ├── chat.store.ts
│   │   │   ├── session.store.ts
│   │   │   └── settings.store.ts
│   │   │
│   │   ├── hooks/            # 自定义 Hooks
│   │   │   ├── useChat.ts
│   │   │   ├── useMemory.ts
│   │   │   └── useMcp.ts
│   │   │
│   │   ├── i18n/             # 国际化
│   │   │   ├── index.ts
│   │   │   └── locales/
│   │   │       ├── zh-CN.json
│   │   │       ├── en-US.json
│   │   │       └── ja-JP.json
│   │   │
│   │   ├── utils/            # 工具函数
│   │   │   ├── format.ts
│   │   │   ├── validation.ts
│   │   │   └── constants.ts
│   │   │
│   │   └── types.ts          # UI 类型定义
│   │
│   ├── shared/               # 共享代码
│   │   ├── types.ts          # 共享类型
│   │   └── constants.ts      # 共享常量
│   │
│   └── types/                # 全局类型
│       └── global.d.ts
│
├── tests/                    # 测试文件
│   ├── unit/                 # 单元测试
│   ├── integration/          # 集成测试
│   └── e2e/                  # 端到端测试
│
├── docs/                     # 文档目录
│   ├── 开发者文档.md
│   ├── 用户使用教程.md
│   └── API参考文档.md
│
├── dist-electron/            # Electron 编译输出
├── dist-react/               # React 构建输出
│
├── electron-builder.json     # Electron Builder 配置
├── vite.config.ts            # Vite 配置
├── tsconfig.json             # TypeScript 配置
├── vitest.config.ts          # Vitest 配置
│
├── package.json              # 项目配置
├── README.md                 # 项目说明
└── LICENSE.txt               # 许可证
```

---

## 核心模块说明

### 1. Electron 主进程 (`src/electron/`)

#### 1.1 主入口 (`main.ts`)

```typescript
/**
 * Electron 主进程入口
 * 负责应用生命周期管理、窗口创建、IPC 通信
 */
import { app, BrowserWindow } from 'electron';
import { registerIpcHandlers } from './ipc-handlers';
import { createLogger } from './logger';

const logger = createLogger();

app.whenReady().then(() => {
  registerIpcHandlers(); // 注册所有 IPC 处理器
  createMainWindow();    // 创建主窗口
});
```

#### 1.2 IPC 处理器 (`ipc-handlers.ts`)

```typescript
/**
 * IPC 事件处理器注册
 * 定义渲染进程与主进程之间的通信接口
 */
export function registerIpcHandlers() {
  // 会话相关
  ipcMain.handle('session:create', handleCreateSession);
  ipcMain.handle('session:list', handleListSessions);
  ipcMain.handle('session:delete', handleDeleteSession);

  // 消息相关
  ipcMain.handle('message:send', handleSendMessage);

  // 记忆系统
  ipcMain.handle('memory:store', handleMemoryStore);
  ipcMain.handle('memory:search', handleMemorySearch);

  // 配置管理
  ipcMain.handle('config:get', handleGetConfig);
  ipcMain.handle('config:set', handleSetConfig);

  // MCP 服务器
  ipcMain.handle('mcp:test', handleTestMcpServer);
  ipcMain.handle('mcp:list', handleListMcpServers);
}
```

#### 1.3 日志系统 (`logger.ts`)

```typescript
/**
 * Winston 日志系统
 * 支持控制台输出和文件日志
 */
import winston from 'winston';

export function createLogger() {
  return winston.createLogger({
    level: 'info',
    format: winston.format.json(),
    transports: [
      new winston.transports.File({ filename: 'logs/error.log', level: 'error' }),
      new winston.transports.File({ filename: 'logs/combined.log' }),
      new winston.transports.Console({ format: winston.format.simple() })
    ]
  });
}
```

### 2. 核心库 (`src/electron/libs/`)

#### 2.1 配置管理 (`config-store.ts`)

```typescript
/**
 * API 配置管理
 * 负责多个 AI API 配置的增删改查
 */
class ConfigStore {
  private db: better_sqlite3.Database;

  // 添加配置
  addConfig(config: ApiConfig): void { }

  // 获取所有配置
  getAllConfigs(): ApiConfig[] { }

  // 设置激活配置
  setActiveConfig(id: string): void { }

  // 获取激活配置
  getActiveConfig(): ApiConfig | null { }

  // 删除配置
  deleteConfig(id: string): void { }
}
```

#### 2.2 会话管理 (`session-store.ts`)

```typescript
/**
 * 会话存储管理
 * 负责聊天会话的持久化
 */
class SessionStore {
  private db: better_sqlite3.Database;

  // 创建会话
  createSession(title: string): Session { }

  // 获取所有会话
  listSessions(): Session[] { }

  // 添加消息
  addMessage(sessionId: string, message: Message): void { }

  // 获取会话消息
  getMessages(sessionId: string): Message[] { }

  // 删除会话
  deleteSession(id: string): void { }
}
```

#### 2.3 记忆系统 (`memory-tools.ts`)

```typescript
/**
 * 记忆系统（双后端实现）
 * 1. Memvid SDK - 向量数据库（语义搜索）
 * 2. FileSystem - 文件系统备份
 */
class MemoryTools {
  private memvid: MemvidClient;
  private fsStorage: FsStorage;

  // 存储记忆
  async store(title: string, text: string, label: MemoryLabel): Promise<void> {
    // 1. 存储到 Memvid
    await this.memvid.store({ title, text, label });

    // 2. 备份到文件系统
    await this.fsStorage.save({ title, text, label });
  }

  // 搜索记忆
  async search(query: string, k: number = 6): Promise<MemoryResult[]> {
    // 使用 Memvid 进行向量搜索
    return await this.memvid.search(query, { topK: k });
  }

  // 基于记忆的问答
  async ask(question: string): Promise<string> {
    const results = await this.search(question);
    // 使用 RAG 技术生成答案
    return this.ragGenerate(question, results);
  }
}
```

#### 2.4 技能管理 (`skills-store.ts`)

```typescript
/**
 * 技能管理
 * 负责自定义技能的加载、解析、执行
 */
class SkillsStore {
  private skillsDir: string;

  // 加载所有技能
  async loadAllSkills(): Promise<Skill[]> {
    const files = await fs.readdir(this.skillsDir);
    return Promise.all(
      files
        .filter(f => f.endsWith('.md'))
        .map(f => this.parseSkill(path.join(this.skillsDir, f)))
    );
  }

  // 解析技能文件
  private async parseSkill(filePath: string): Promise<Skill> {
    const content = await fs.readFile(filePath, 'utf-8');
    const frontmatter = parseFrontmatter(content);
    return {
      name: frontmatter.name,
      description: frontmatter.description,
      prompt: content.replace(/---[\s\S]*?---/, ''),
      parameters: extractParameters(content)
    };
  }

  // 执行技能
  async executeSkill(name: string, params: Record<string, any>): Promise<string> {
    const skill = await this.getSkill(name);
    const prompt = replaceParameters(skill.prompt, params);
    return prompt; // 返回处理后的提示词
  }
}
```

#### 2.5 MCP 服务器管理 (`mcp-store.ts`)

```typescript
/**
 * MCP 服务器管理
 * 负责 MCP 服务器的注册、启动、测试
 */
class McpStore {
  private servers: Map<string, McpServerConfig>;

  // 添加服务器
  addServer(config: McpServerConfig): void {
    this.servers.set(config.id, config);
  }

  // 测试连接
  async testConnection(config: McpServerConfig): Promise<boolean> {
    try {
      const process = spawn(config.command, config.args, {
        env: { ...process.env, ...config.env }
      });
      // 测试逻辑...
      return true;
    } catch (error) {
      return false;
    }
  }

  // 启动服务器
  async startServer(id: string): Promise<void> {
    const config = this.servers.get(id);
    if (!config) throw new Error('Server not found');

    const process = spawn(config.command, config.args, {
      env: { ...process.env, ...config.env }
    });

    // 监听进程输出...
  }
}
```

### 3. React 渲染进程 (`src/ui/`)

#### 3.1 状态管理 (`store/`)

```typescript
/**
 * Zustand 状态管理
 * 轻量级、简洁的状态管理方案
 */
import { create } from 'zustand';

interface ChatStore {
  // 状态
  sessions: Session[];
  currentSessionId: string | null;
  messages: Message[];

  // 操作
  createSession: (title: string) => void;
  sendMessage: (content: string) => Promise<void>;
  deleteSession: (id: string) => void;
  setCurrentSession: (id: string) => void;
}

export const useChatStore = create<ChatStore>((set, get) => ({
  // 初始状态
  sessions: [],
  currentSessionId: null,
  messages: [],

  // 操作实现
  createSession: async (title) => {
    const session = await window.api.session.create(title);
    set((state) => ({ sessions: [...state.sessions, session] }));
  },

  sendMessage: async (content) => {
    const { currentSessionId } = get();
    if (!currentSessionId) return;

    // 添加用户消息
    const userMessage: Message = { role: 'user', content };
    set((state) => ({ messages: [...state.messages, userMessage] }));

    // 发送到主进程
    const response = await window.api.message.send(currentSessionId, content);

    // 添加助手消息
    const assistantMessage: Message = { role: 'assistant', content: response };
    set((state) => ({ messages: [...state.messages, assistantMessage] }));
  },

  deleteSession: async (id) => {
    await window.api.session.delete(id);
    set((state) => ({
      sessions: state.sessions.filter(s => s.id !== id)
    }));
  },

  setCurrentSession: (id) => {
    set({ currentSessionId: id });
    // 加载会话消息...
  }
}));
```

#### 3.2 自定义 Hooks (`hooks/`)

```typescript
/**
 * useChat Hook
 * 封装聊天相关逻辑
 */
export function useChat() {
  const { sessions, currentSessionId, messages, createSession, sendMessage } = useChatStore();

  const sendMessage = async (content: string) => {
    if (!currentSessionId) {
      // 自动创建会话
      const newSession = await createSession('新对话');
      setCurrentSession(newSession.id);
    }

    await sendMessage(content);
  };

  return {
    sessions,
    currentSessionId,
    messages,
    sendMessage: handleSendMessage
  };
}

/**
 * useMemory Hook
 * 封装记忆系统操作
 */
export function useMemory() {
  const [memories, setMemories] = useState<Memory[]>([]);

  const searchMemory = async (query: string) => {
    const results = await window.api.memory.search(query, 6);
    setMemories(results);
  };

  const storeMemory = async (title: string, text: string, label: MemoryLabel) => {
    await window.api.memory.store(title, text, label);
  };

  return {
    memories,
    searchMemory,
    storeMemory
  };
}
```

### 4. API 代理层 (`src/electron/api-proxy/`)

```typescript
/**
 * AI API 代理
 * 统一处理多个 AI 提供商的 API 调用
 */
class ApiProxy {
  private providers: Map<string, ApiProvider>;

  async sendMessage(messages: Message[], config: ApiConfig): Promise<string> {
    const provider = this.providers.get(config.provider);
    if (!provider) throw new Error('Provider not found');

    // 流式响应
    const stream = await provider.stream(messages, {
      apiKey: config.apiKey,
      model: config.model,
      baseUrl: config.baseUrl
    });

    let fullResponse = '';
    for await (const chunk of stream) {
      fullResponse += chunk;
      // 实时发送到渲染进程
      mainWindow?.webContents.send('message:chunk', chunk);
    }

    return fullResponse;
  }
}
```

---

## 开发指南

### 添加新的 AI 提供商

#### 1. 定义提供商配置

编辑 `src/electron/config/providers.ts`:

```typescript
export const API_PROVIDERS = {
  // 新增提供商
  newprovider: {
    name: '新提供商',
    baseUrl: 'https://api.newprovider.com/v1',
    models: ['model-1', 'model-2'],
    requiredFields: ['apiKey', 'model']
  }
};
```

#### 2. 实现提供商服务

创建 `src/electron/services/newprovider.service.ts`:

```typescript
export class NewProviderService implements ApiProvider {
  async stream(messages: Message[], options: RequestOptions): AsyncIterable<string> {
    const response = await fetch(`${this.baseUrl}/chat/completions`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${options.apiKey}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        model: options.model,
        messages,
        stream: true
      })
    });

    return this.parseStream(response);
  }
}
```

#### 3. 注册提供商

编辑 `src/electron/api-proxy/index.ts`:

```typescript
import { NewProviderService } from '../services/newprovider.service';

this.providers.set('newprovider', new NewProviderService());
```

### 添加新的斜杠命令

#### 1. 定义命令处理器

编辑 `src/electron/ipc-handlers.ts`:

```typescript
ipcMain.handle('command:mycommand', async (event, args) => {
  // 实现命令逻辑
  return { success: true, data: 'result' };
});
```

#### 2. 添加类型定义

编辑 `src/electron/preload.cts`:

```typescript
export interface Api {
  command: {
    mycommand: (args: any) => Promise<Result>;
  }
}
```

#### 3. UI 集成

在聊天输入框中解析 `/mycommand`：

```typescript
const handleCommand = (input: string) => {
  if (input.startsWith('/mycommand')) {
    const args = parseCommandArgs(input);
    const result = await window.api.command.mycommand(args);
    return result;
  }
};
```

### 创建自定义技能

#### 1. 创建技能文件

在 `~/.claude/skills/` 目录创建 `my-skill.md`:

```markdown
---
name: my-skill
description: 我的自定义技能
author: Your Name
version: 1.0.0
---

# 技能提示词内容

你是一个专业的[角色]，你的任务是[任务描述]。

## 使用方法

用户输入：{{input}}

## 输出格式

请按照以下格式输出：
1. ...
2. ...
3. ...
```

#### 2. 使用技能

在聊天输入框输入：

```
/input my-skill --input="用户输入内容"
```

### 开发自定义 MCP 服务器

#### 1. 创建 MCP 服务器

参考官方文档创建 MCP 服务器：https://modelcontextprotocol.io/

#### 2. 在 AICowork 中配置

打开设置 → MCP 服务器 → 添加服务器：

```json
{
  "id": "my-mcp-server",
  "name": "我的 MCP 服务器",
  "command": "node",
  "args": ["path/to/your/server.js"],
  "env": {
    "API_KEY": "your-api-key"
  }
}
```

---

## 测试指南

### 单元测试

使用 Vitest 运行单元测试：

```bash
# 运行所有测试
pnpm test

# 监听模式
pnpm test:watch

# 生成覆盖率报告
pnpm test:coverage
```

#### 示例测试

创建 `tests/unit/memory-tools.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { MemoryTools } from '../../src/electron/libs/memory-tools';

describe('MemoryTools', () => {
  it('should store memory', async () => {
    const memory = new MemoryTools();
    await memory.store('测试', '这是测试内容', 'custom');

    const results = await memory.search('测试');
    expect(results).toHaveLength(1);
    expect(results[0].title).toBe('测试');
  });
});
```

### 集成测试

创建 `tests/integration/api-test.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test('API 配置测试', async ({ page }) => {
  await page.goto('http://localhost:5173');

  // 打开设置
  await page.click('[data-testid="settings-button"]');

  // 添加 API 配置
  await page.click('[data-testid="add-config-button"]');
  await page.fill('[name="configName"]', '测试配置');
  await page.fill('[name="apiKey"]', 'sk-test');
  await page.selectOption('[name="provider"]', 'openai');
  await page.click('[data-testid="save-button"]');

  // 验证配置已添加
  await expect(page.locator('text=测试配置')).toBeVisible();
});
```

---

## 构建与打包

### 开发构建

```bash
# 仅构建 React
pnpm vite:build

# 构建 Electron
pnpm transpile:electron

# 完整构建
pnpm build
```

### 打包应用

```bash
# Windows
pnpm dist:win

# macOS ARM
pnpm dist:mac-arm64

# macOS Intel
pnpm dist:mac-x64

# Linux
pnpm dist:linux
```

### 输出文件

打包完成后，安装包位于 `dist/` 目录：

- Windows: `dist/AICowork-win32-x64-0.1.0.exe`
- macOS: `dist/AICowork-0.1.0-arm64.dmg`
- Linux: `dist/AICowork-0.1.0.AppImage`

---

## 调试技巧

### Electron 调试

#### 1. 开启开发者工具

主进程中添加：

```typescript
mainWindow.webContents.openDevTools();
```

或按快捷键：
- **Windows/Linux**: `Ctrl + Shift + I`
- **macOS**: `Cmd + Option + I`

#### 2. 查看 Chrome 日志

```bash
# Windows
set ELECTRON_ENABLE_LOGGING=1

# macOS
export ELECTRON_ENABLE_LOGGING=1

# Linux
ELECTRON_ENABLE_LOGGING=1
```

#### 3. 调试主进程

在 VS Code 中创建 `.vscode/launch.json`:

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Main Process",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/electron",
      "windows": {
        "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/electron.cmd"
      },
      "args": ["."],
      "outputCapture": "std"
    }
  ]
}
```

### React 调试

#### 1. React DevTools

安装 Chrome 扩展：
- [React Developer Tools](https://chrome.google.com/webstore/detail/react-developer-tools/fmkadmapgofadopljbjfkapdkoienihi)

#### 2. Redux DevTools（如果使用 Redux）

查看 Zustand 状态：

```typescript
// 在控制台中
const state = useChatStore.getState();
console.log(state);
```

#### 3. 网络调试

在 Network 标签页查看：
- IPC 通信（main world）
- API 请求（渲染进程）

---

## 常见问题

### Q1: `better-sqlite3` 编译失败

**问题**: 安装依赖时出现编译错误

**解决方案**:

```bash
# 1. 安装构建工具
# Windows
npm install -g windows-build-tools

# macOS
xcode-select --install

# Linux
sudo apt-get install build-essential

# 2. 重建原生模块
pnpm rebuild
```

### Q2: Electron 启动失败

**问题**: Electron failed to install correctly

**解决方案**:

```bash
# 清除缓存
rm -rf node_modules/electron
pnpm install

# 或使用 electron-fix
npm install -g electron-fix
electron-fix start
```

### Q3: IPC 通信失败

**问题**: `Error: IPC channel is closed`

**解决方案**:

- 确保在 `app.whenReady()` 之后注册 IPC 处理器
- 检查 `contextBridge` 配置
- 验证频道名称拼写正确

### Q4: 热重载不工作

**问题**: 修改代码后页面不自动刷新

**解决方案**:

```bash
# 确保使用开发模式
pnpm dev

# 检查 Vite 配置
# vite.config.ts
server: {
  watch: {
    usePolling: true
  }
}
```

### Q5: 打包后应用无法启动

**问题**: 双击 exe 文件无响应

**解决方案**:

1. 检查 `electron-builder.json` 配置
2. 验证 `main` 字段指向正确的入口
3. 查看打包日志：`pnpm dist --verbose`

---

## 贡献指南

### 代码规范

- 使用 ESLint 进行代码检查
- 遵循 TypeScript 严格模式
- 编写单元测试
- 添加 JSDoc 注释

### 提交规范

遵循 Conventional Commits:

```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型**:
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档变更
- `style`: 代码格式
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试
- `chore`: 构建/工具

### Pull Request

1. Fork 项目
2. 创建特性分支
3. 提交变更
4. 推送到分支
5. 创建 Pull Request

---

## 资源链接

### 官方文档

- [Electron 文档](https://www.electronjs.org/docs)
- [React 文档](https://react.dev)
- [TypeScript 文档](https://www.typescriptlang.org/docs)
- [Vite 文档](https://vitejs.dev)

### 相关项目

- [Anthropic Claude SDK](https://github.com/anthropics/anthropic-sdk-typescript)
- [Memvid SDK](https://memvid.ai/)
- [MCP 协议](https://modelcontextprotocol.io/)
- [Zustand](https://zustand-demo.pmnd.rs/)

---

**文档维护**: Alan
**最后更新**: 2026-01-24
**许可证**: AGPL-3.0
